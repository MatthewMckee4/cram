# Writing Cards

Card content is written in [Typst](https://typst.app) markup.

## Example

```typst
= What is ownership in Rust?

Every value has a single *owner*. When the owner goes out of scope, the value is dropped.
```

## Math

```typst
$ sum_(i=0)^n i = (n(n+1))/2 $
```
